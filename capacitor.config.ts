import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.davidemaggi.myittlecora',
  appName: 'MyLittleCora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
